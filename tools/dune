(executable
  (name Request)
  (public_name request)
  (modules Request)
  (libraries httpkit logs.fmt fmt.tty))

(executable
  (name EchoServer)
  (modules EchoServer)
  (public_name echottp)
  (libraries httpkit logs.fmt fmt.tty))
